// math.min is a predefined function that returns the smallest of zero or more numbers.
// math.max is a predefined function that returns the largest of zero or more numbers.
let max=Math.max(3,5,10);
console.log(max);//10
let min=Math.min(3,5,10);
console.log(min);//3
